import React from 'react';

// This file intentionally exports a harmless component so the fast-refresh
// rule doesn't complain about non-component exports in .jsx files. The
// real AppContext is defined in `AppContext.js` and should be imported
// from there.
export default function _AppContextPlaceholder() {
	return null;
}
